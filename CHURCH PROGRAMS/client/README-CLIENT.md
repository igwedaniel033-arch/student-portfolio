# Client (React + Vite)

Quick start (dev):

```powershell
cd "c:\Users\User\CHURCH PROGRAMS\client"
npm install
npm run dev
```

This frontend expects the backend at `http://localhost:3000` for API and Socket.IO.
